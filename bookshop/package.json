{
  "name": "bookshop",
  "version": "1.0.0",
  "description": "A simple CAP project.",
  "repository": "<Add your repository here>",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "@sap/cds": "^3",
    "express": "^4",
    "sqlite3": "^5.0.0"
  },
  "scripts": {
    "start": "npx cds run",
    "test": "jest"
  },
  "cds": {
    "requires": {
      "db": {
        "kind": "sqlite"
      }
    }
  },
  "devDependencies": {
    "jest": "^26.4.0",
    "supertest": "^4.0.2"
  }
}
